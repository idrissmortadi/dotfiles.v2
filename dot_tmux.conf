# Terminal setup
set-option -sa terminal-overrides ",xterm*:Tc"
set -g default-terminal "tmux-256color"

# Status and mouse
set-option -g status-position top
set -g mouse on

# Prefix key
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Vim-style pane selection
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Start windows and panes at 1
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Alt-arrow keys to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrows to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift+Alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

# ================= TPM Plugins =================
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'

# Load TPM.
run '~/.tmux/plugins/tpm/tpm'
# ===============================================

# Custom status line theme
set -g status-style "bg=default,fg=#000000"
set -g status-justify left

# Session name with rounded arrows on the left
set -g status-left "#[fg=#698DDA]#[bg=#698DDA,fg=#000000]#S#[fg=#698DDA,bg=default]"
set -g status-left-length 50

# Window status format (inactive windows #I:#W)
set -g window-status-format " #[fg=#344775]#[bg=#344775,fg=#000000] #I:#W #[fg=#344775,bg=default]"

# Window status format (active window with rounded arrows)
set -g window-status-current-format " #[fg=#698DDA]#[bg=#698DDA,fg=#000000] #I:#W #[fg=#698DDA,bg=default]"

set -g status-right ""
set -g status-right-length 0

# vi-mode
set-window-option -g mode-keys vi

# Copy mode keybindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# Split windows with current path
bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
